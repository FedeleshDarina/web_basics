//Лекція 2

//Завдання 1 
var foo = 1;
function bar() {
  if (!foo) {
    var foo = 10;
  }
  alert(foo);
}
bar();
//Завдання 3
var a = 1; //Змінній а задли значення 1
function b() {
  a = 10;
  return; //Створили змінну b в середині якої є змінна а, але так як функція завершується то вона ні на що не впливає
  function a() { } //оголошення функціїї а
}
b(); //підчас виклику b локальна а змінює своє значення на 10, але на глобальну це ніяк не вплинуло
console.log(a); //вивиедення а
